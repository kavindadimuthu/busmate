# Base URL of the api-gateway (which hosts the BFF auth module at /api/bff/auth/*).
# Standard local gateway port is 8080 — run `pnpm dev:api-gateway` (now includes
# the BFF module) before starting this app.
VITE_GATEWAY_URL=http://localhost:8080
