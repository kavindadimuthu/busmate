package com.busmate.routeschedule.fleet.enums;

/** Scanned documents a bus operator is expected to keep for each vehicle (INC-018). */
public enum BusDocumentTypeEnum {
    REGISTRATION_CERTIFICATE,
    REVENUE_LICENCE,
    INSURANCE,
    FITNESS_CERTIFICATE,
    EMISSION_CERTIFICATE,
    ROUTE_PERMIT,
    OTHER
}
